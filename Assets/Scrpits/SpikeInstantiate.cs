using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpikeInstantiate : MonoBehaviour
{
    public GameObject gO_Spike;
    // Start is called before the first frame update
    void Start()
    {
        //GameObject a = new GameObject(gO_Spike) as GameObject;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
